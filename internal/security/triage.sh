#!/usr/bin/env bash
set -euo pipefail
# Emergency triage shell for PromptOS recovery flows.
# This is a restricted rescue shell entrypoint.
# Implementation placeholder — extend in next task.

exec /bin/bash --noprofile --norc "$@"
